import { useParams,useNavigate } from 'react-router-dom';import { AppShell } from '../components/AppShell';import { useAppStore } from '../stores/appStore';
export default function MedicalFormPage(){const {id=''}=useParams();const up=useAppStore(s=>s.updateVisitStatus);const nav=useNavigate();return <AppShell title='Pemeriksaan'><textarea className='w-full border rounded p-2' placeholder='Anamnesis'/><div className='grid gap-2 mt-3'><button className='bg-orange-500 text-white p-3 rounded-xl' onClick={()=>{up(id,'Farmasi');nav('/pharmacy');}}>Simpan & Lanjut ke Farmasi</button><button className='bg-green-600 text-white p-3 rounded-xl' onClick={()=>{up(id,'Selesai');nav('/services');}}>Simpan & Selesaikan</button></div></AppShell>}
