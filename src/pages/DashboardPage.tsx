import { AppShell } from '../components/AppShell';import { useAppStore } from '../stores/appStore';import { summarize } from '../lib/report';
export default function DashboardPage(){const d=useAppStore(s=>s.data);const r=summarize(d);return <AppShell title='Dashboard Lapangan'><div className='bg-white p-3 rounded-xl border'>{d.activity.name}<div className='text-sm text-slate-500'>{d.activity.location} · {d.activity.date}</div></div><div className='grid grid-cols-2 gap-2 text-sm'>{Object.entries(r).map(([k,v])=><div key={k} className='bg-white border rounded-xl p-3'><div>{k}</div><b>{v}</b></div>)}</div></AppShell>}
